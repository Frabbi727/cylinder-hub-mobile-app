name: cylinder_hub_mobile_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.1+2

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  get: ^4.6.6
  dio: ^5.4.1
  logger: ^2.0.2+1
  get_storage: ^2.1.1
  package_info_plus: ^10.1.0
  connectivity_plus: ^7.1.1
  cupertino_icons: ^1.0.8
  equatable: ^2.0.5
  json_annotation: ^4.9.0
  intl: ^0.20.2
  fl_chart: ^0.68.0
  shimmer: ^3.0.0
  firebase_core: ^4.10.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  build_runner: ^2.4.13
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
